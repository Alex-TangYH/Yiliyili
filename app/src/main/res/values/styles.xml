<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--粉色主题-->
    <style name="AppPinkTheme" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/bili_color_pink</item>
        <item name="colorPrimaryDark">@color/bili_color_darkpink</item>
        <item name="android:windowBackground">@color/bili_color_light_gray</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="theme_fullScreen" parent = "AppPinkTheme">
        <item name="android:windowFullscreen">true</item>
    </style>


</resources>
